# ── OpenAI (Stage 2+ 에서 사용) ──
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBED_MODEL=text-embedding-3-small

# ── RAG 파라미터 ──
RETRIEVAL_TOP_K=5
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# ── Mock 모드 (true: API 키 없이 더미 데이터로 동작) ──
MOCK_MODE=true
